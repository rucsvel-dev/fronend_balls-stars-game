import React from 'react';
import { Switch, Route } from 'react-router-dom';

export const AppContainer: React.FC<{}> = () => {
    return (
        <>
            <Switch>

            </Switch>
        </>
    );
};
