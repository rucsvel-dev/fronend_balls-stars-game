import React, { FC } from 'react';

import {LoadingProps} from "@modules/Common/components/loading/loadingTypes";

export const Loading: FC<LoadingProps> = ({}) => {
    return (
        <div>loading</div>
    );
};
